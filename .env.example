# Local development only. These are throwaway values used by docker-compose
# on localhost. They are NOT secrets and must never be reused anywhere else.
DATABASE_URL=postgres://postgres:postgres@localhost:5433/invoices?sslmode=disable
HTTP_ADDR=:8080
